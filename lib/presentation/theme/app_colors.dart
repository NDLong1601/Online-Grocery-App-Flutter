import 'package:flutter/material.dart';

/// A class that defines the color scheme used throughout the application.
///
/// This class contains static constant colors that maintain consistency
/// across the app's UI components and follow the app's design system.
class AppColors {
  /// The primary color used for main UI elements, buttons, and branding.
  /// Value: #0A8754 (Green)
  static const Color primaryColor = Color(0xFF0A8754);

  /// The secondary color used for accent elements and highlights.
  /// Value: #FFC107 (Amber/Yellow)
  static const Color secondaryColor = Color(0xFFFFC107);

  /// The default background color for screens and containers.
  /// Value: #F5F5F5 (Light Gray)
  static const Color backgroundColor = Color(0xFFF5F5F5);

  /// The primary text color used for readability and contrast.
  /// Value: #212121 (Dark Gray)
  static const Color textColor = Color(0xFF212121);

  /// Additional color palette
  /// Value: #53B175 (Green)
  static const Color greenAccent = Color(0xFF53B175);

  /// Value: #5383EC (Blue)
  static const Color blueAccent = Color(0xFF5383EC);

  /// Value: #4A66AC (Dark Blue)
  static const Color darkBlueAccent = Color(0xFF4A66AC);

  /// Value: #181725 (Dark Text)
  static const Color darkText = Color(0xFF181725);

  /// Value: #7C7C7C (Gray Text)
  static const Color grayText = Color(0xFF7C7C7C);

  /// Value: #F7A593 (Light Salmon)
  static const Color salmonLight = Color(0xFFF7A593);

  /// Value: #F8A44C (Orange)
  static const Color orange = Color(0xFFF8A44C);

  /// Value: #D3B0E0 (Light Purple)
  static const Color purpleLight = Color(0xFFD3B0E0);

  /// Value: #B7DFF5 (Light Blue)
  static const Color lightBlue = Color(0xFFB7DFF5);

  /// Value: #FDE598 (Light Yellow)
  static const Color lightYellow = Color(0xFFFDE598);

  /// Value: #F3603F (Coral)
  static const Color coral = Color(0xFFF3603F);

  /// Value: #FFFFFF (White)
  static const Color white = Color(0xFFFFFFFF);
}
